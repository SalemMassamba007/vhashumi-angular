import { CountOnEnter } from './count-on-enter';

describe('CountOnEnter', () => {
  it('should create an instance', () => {
    const directive = new CountOnEnter();
    expect(directive).toBeTruthy();
  });
});
